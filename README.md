# Text-Classification-TF-IDF
A pipeline that takes raw text (e.g., movie reviews), cleans it, converts it into numbers (vectors), and feeds it into a machine learning model to predict a label (e.g., Positive/Negative).
